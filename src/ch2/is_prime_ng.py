# 引数nが素数かどうかを調べる関数(未完成)
def is_prime(n):
    # TODO: ここに実装を作る
    return False

# 関数is_primeをテストする関数 --- (*1)
def test_is_prime():
    assert is_prime(3) == True
    assert is_prime(8) == False
    assert is_prime(89) == True

